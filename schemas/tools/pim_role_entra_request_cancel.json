{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "items": {
      "description": "Optional preselected pending requests. When omitted the user picks from all pending requests in the browser form.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "requestId": {
            "type": "string",
            "minLength": 1,
            "description": "The pending request id from pim_role_entra_request_list."
          }
        },
        "required": [
          "requestId"
        ],
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
